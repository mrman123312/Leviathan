name: Apply strength v8.2 proof certificates

on:
  push:
    branches:
      - leviathan/strength-v8-evidence-lattice
    paths:
      - '.github/workflows/apply-strength-v8-2-certificates.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout strength v8
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: leviathan/strength-v8-evidence-lattice

      - name: Apply depth-monotonic proof certificates
        run: |
          python3 tools/leviathan_apply_v8_2_certificates.py
          git diff --check
          git diff -- src/search.h src/search.cpp

      - name: Commit proof certificates
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/search.h src/search.cpp
          git commit -m 'Strength v8.2: add depth-monotonic proof certificates'
          git push origin HEAD:leviathan/strength-v8-evidence-lattice
